{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "rower-prototype",
      "runtimeExecutable": "node",
      "runtimeArgs": [
        "-e",
        "const http=require('http'),fs=require('fs'),p=require('path');const root=p.resolve(process.cwd(),'prototype');http.createServer((q,s)=>{let u=q.url==='/'?'/index.html':q.url.split('?')[0];const f=p.resolve(root,'.'+decodeURIComponent(u));if(f!==root&&!f.startsWith(root+p.sep)){s.writeHead(403);s.end('forbidden');return;}fs.readFile(f,(e,d)=>{if(e){s.writeHead(404);s.end('not found');}else{s.writeHead(200,{'Content-Type':'text/html; charset=utf-8'});s.end(d);}});}).listen(4173,()=>console.log('rower prototype on http://localhost:4173'));"
      ],
      "port": 4173
    }
  ]
}
